import React from 'react';

export default function MySubmissions() {
  return (
    <div style={{ background: '#fff', padding: '3rem', borderRadius: '12px', borderTop: '6px solid #0284c7', boxShadow: '0 2px 8px rgba(0,0,0,0.03)' }}>
      <h1 style={{ color: '#333', marginBottom: '10px' }}>My Submissions</h1>
      <p style={{ color: '#666' }}>This is where Ken checks if the admin approved or denied his encoded data.</p>
    </div>
  );
}