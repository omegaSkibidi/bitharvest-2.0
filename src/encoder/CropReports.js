import React from 'react';

export default function CropReports() {
  return (
    <div style={{ background: '#fff', padding: '3rem', borderRadius: '12px', borderTop: '6px solid #854d0e', boxShadow: '0 2px 8px rgba(0,0,0,0.03)' }}>
      <h1 style={{ color: '#333', marginBottom: '10px' }}>Crop Reports (Data Entry)</h1>
      <p style={{ color: '#666' }}>This is where you will build the forms to encode new municipal data.</p>
    </div>
  );
}