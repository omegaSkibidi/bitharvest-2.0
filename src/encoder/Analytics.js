import React from 'react';

export default function Analytics() {
  return (
    <div style={{ background: '#fff', padding: '3rem', borderRadius: '12px', borderTop: '6px solid #9333ea', boxShadow: '0 2px 8px rgba(0,0,0,0.03)' }}>
      <h1 style={{ color: '#333', marginBottom: '10px' }}>Personal Analytics</h1>
      <p style={{ color: '#666' }}>This is where the user sees their own performance charts.</p>
    </div>
  );
}