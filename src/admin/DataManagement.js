import React from 'react';

export default function DataManagement() {
  return (
    <div style={{ background: '#fff', padding: '3rem', borderRadius: '12px', borderTop: '6px solid #2563EB', boxShadow: '0 2px 8px rgba(0,0,0,0.03)' }}>
      <h1 style={{ color: '#333', marginBottom: '10px' }}>Data Management</h1>
      <p style={{ color: '#666' }}>This is where the admin manages encoder accounts, crop categories, and system-wide data settings.</p>
    </div>
  );
}